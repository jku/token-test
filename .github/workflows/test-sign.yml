name: CI
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v3

      - name: Prep...
        run: echo "prep $test_var"

      - name: Testing...
        if: vars.TEST_VARIABLE 
        run: |
          echo "running"
          echo "$test_var"
