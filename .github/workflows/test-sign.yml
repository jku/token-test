name: Test signing
on:
  workflow_dispatch:
  push:
    branches:
    - testbranch
    paths:
    - 'testdir/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo event_name: ${{ github.event_name }}
