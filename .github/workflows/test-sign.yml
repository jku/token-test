name: Test signing
on:
  workflow_dispatch:
  push:
    branches:
    - testbranch
    paths:
    - 'testdir/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          git tag -a v0.1 -m "test tag protection"
          git push --tags
